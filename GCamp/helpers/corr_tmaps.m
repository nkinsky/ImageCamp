function [ corrs ] = corr_tmaps( TMap1,TMap2 )
% corrs = corr_tmaps( TMap1,TMap2 )
%   Performs a spatial correlation of all TMaps in TMap1 and TMap2 (must be
%   the same size cells)

num_cells = length(TMap1);

for j = 1:numcells


end

