% registration scratchpad

%% Get distribution of multiple mapping neurons across all sessions in batch_session map
batch_map = importdata('batch_session_map.mat');

for j = 1:length(batch_map.session)
    
    
